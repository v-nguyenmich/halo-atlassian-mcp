name: weekly-rebuild

on:
  schedule:
    - cron: "17 7 * * 1"  # Monday 07:17 UTC
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  rebuild:
    uses: ./.github/workflows/ci.yml
    with:
      no_cache: true
    secrets: inherit
